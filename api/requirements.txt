# ============ Web 框架 ============
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ============ 数据校验 ============
pydantic==2.9.2
pydantic-settings==2.5.2

# ============ 数据库 ============
SQLAlchemy==2.0.35
aiomysql==0.2.0
aiosqlite==0.20.0      # 本地开发用 SQLite 零依赖
PyMySQL==1.1.1
asyncpg==0.29.0        # PostgreSQL 异步驱动（Render 免费层 PG）
psycopg2-binary==2.9.9 # PostgreSQL 同步驱动（alembic 用）
alembic==1.13.3
cryptography==43.0.1

# ============ Redis ============
redis==5.0.8

# ============ 安全 ============
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ============ 工具 ============
python-dotenv==1.0.1
python-multipart==0.0.12
httpx==0.27.2

# ============ 微信支付（v3.1: wechatpy-pay 不可用，移至可选，支付留待接入）============
# wechatpy-pay==1.8.18  # 暂未找到稳定源，先注释
wechatpy==1.8.18

# ============ 二维码 / 海报 ============
qrcode==7.4.2
Pillow==10.4.0

# ============ 测试 ============
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ============ 部署 / 监控 ============
gunicorn==23.0.0
