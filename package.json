{
  "name": "xincetong-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel 入口 wrapper — 实际构建在 xincetong-web/",
  "scripts": {
    "build": "cd xincetong-web && npm install --include=dev && npm run build && echo \"✅ xincetong-web 构建完成\""
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
