{
  "name": "xinceutong-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel 入口 wrapper — 实际构建在 xinceutong-web/",
  "scripts": {
    "build": "cd xinceutong-web && npm install --include=dev && npm run build && echo \"✅ xinceutong-web 构建完成\""
  }
}
