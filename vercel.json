{
  "buildCommand": "cd xinceutong-web && npm install --include=dev && npm run build",
  "outputDirectory": "xinceutong-web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
